# AIRchetipo Lite - Greenfield
# Fast implementation path with tech-spec planning (1-15 stories typically)

method_name: "AIRchetipo Lite"
track: "quick-flow"
field_type: "greenfield"
description: "Fast tech-spec based implementation for greenfield projects"

phases:
  - phase: 0
    name: "Discovery (Optional)"
    optional: true
    note: "User-selected during workflow-init"
    workflows:
      - id: "brainstorm-project"
        optional: true
        agent: "strategist"
        command: "brainstorm-project"
        included_by: "user_choice"

      - id: "research"
        optional: true
        agent: "strategist"
        command: "research"
        included_by: "user_choice"
        note: "Can have multiple research workflows"

  - phase: 1
    name: "Planning"
    required: true
    workflows:
      - id: "tech-spec"
        required: true
        agent: "pm"
        command: "tech-spec"
        output: "Technical Specification with stories (auto-detects epic if 2+ stories)"
        note: "Quick Spec Flow - implementation-focused planning ready for handoff"
