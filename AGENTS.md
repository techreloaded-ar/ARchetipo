# AGENTS.md

This file provides guidance to AI agents when working with code in this repository.

## Project

ARchetipo is a set of skills for AI coding agents (Claude Code, Codex, Cursor, Gemini CLI, OpenCode, and GitHub Copilot) that supports the software project ideation, analysis, and planning process.

## Repository structure

```text
skills/                  # Main skills (one directory per skill)
  <skill-name>/
    SKILL.md             # Skill definition
    references/          # Supporting files loaded by the skill
skills-extra/            # Extra skills (same structure)
.archetipo/              # Files installed in the target project (mirrors target structure)
  config.yaml            # Configuration template for the target project
  shared-runtime.md      # Shared rules (Language Policy, Persona, etc.)
cli/                     # Go module implementing the `archetipo` CLI
  cmd/archetipo/         # Binary entry point
  internal/
    cli/                 # Cobra subcommands (public CLI surface)
    domain/              # Shared data types
    connector/           # Interface and implementations (filefs, github)
    config/              # `.archetipo/config.yaml` loader
    iox/                 # JSON envelope for stdin/stdout/stderr
    workspace/           # Workspace initialization: tool/connector registry, config rendering, atomic init
npm/                     # npm package (@techreloaded/archetipo + 6 platform packages)
scripts/                 # npm package build and publishing scripts
```

## Connector architecture

Skills do not manage persistence directly and do not perform connector operations by interpreting instructions. The flow is always:

1. The skill reads `.archetipo/shared-runtime.md` for the JSON envelope, error rules, and invocation discipline.
2. The skill invokes `archetipo <subcmd>` (the Go binary installed globally through `npm i -g @techreloaded/archetipo`).
3. The CLI reads `.archetipo/config.yaml`, selects the connector (`file` or `github`), and performs the operation deterministically.

Skills must explicitly include only the CLI subcommands they actually use, together with their payloads, expected envelopes, and relevant `error.code` values. There is no separate file describing the entire protocol.

## Rules for skill authors

- Call only the subcommands the skill actually uses.
- Content templates (PRDs, story bodies, plan bodies, and sub-issue bodies) are produced by the skill and passed to the CLI through stdin. The CLI persists the payload without enriching it.
- Validation and post-processing of JSON output belong in the skill.
- No-op subcommands are explicit. For example, `comment post` returns `ok: true` with the `file` connector as well. A skill must never branch on connector type.
- Branch on the JSON envelope's `error.code`, not on `message`.
- Load `.archetipo/shared-runtime.md` **exactly once** when the skill starts.

## Rules for CLI changes

- The 14 public CLI operations are stable. Any incompatible change is a breaking change and must be versioned accordingly.
- Keep the conformance suite (`cli/internal/connector/conformance/`) green for all implementations: file, github, and inmemory.
- All GitHub connector GraphQL queries live in `cli/internal/connector/github/templates.go`. Add snapshot tests before modifying them.
- Distribution: the binary and skills share one repository tag. For `v*` tags, `release.yml` runs GoReleaser to produce binaries in `cli/dist/`; `scripts/build-npm.mjs` then copies them into the six `@techreloaded/archetipo-{os}-{arch}` packages and copies the skills into the main `@techreloaded/archetipo` package; finally, `scripts/publish-npm.mjs` publishes all seven npm packages.
- **Before delivering changes**, run the same checks as CI locally:

  ```bash
  cd cli
  gofmt -l .          # must produce no output
  go vet ./...        # must report no errors
  go build ./...      # must compile cleanly
  go test ./...       # all tests must pass
  golangci-lint run --timeout 5m ./...   # 0 issues
  ```

  If `golangci-lint` is not installed, run `go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest`.

## Git commits

- Never add `Co-authored-by` trailers or otherwise mark commits as co-authored.
- Preserve the repository's existing commit style and do not add AI attribution to commit messages.

## E2E tests (`test/e2e/`)

This repository includes a local Node.js E2E harness that exercises the CLI built from source and, for selected scenarios, a real AI agent.

### Main runner

- Command: `npm run test:e2e` (equivalent to `node ./test/e2e/run.mjs`).
- Useful options: `--scenario <id>` / `--scenarios <id1,id2>`, `--config <path>`, and `--timeout-ms <ms>`.
  - Example: `npm run test:e2e -- --scenario worktree-from-plan-to-implement-integrate`.
- The runner always builds the Go CLI into `test/e2e/.bin/archetipo` (`go build -o ... ./cmd/archetipo`), so Go must be installed.
- For every scenario, it creates a sandbox under `test/workspaces/<scenario>/runs/<timestamp>/sandbox`, copies the CLI into `sandbox/bin/`, sets `ARCHETIPO_DATA_DIR` to the repository root, and prepends `sandbox/bin` to `PATH`.
- Every run produces `report.html` and `summary.json` in its run directory. Generated workspaces and the E2E binary (`test/workspaces/*`, `test/e2e/.bin/`) are ignored and must not be committed.
- The default timeout is 20 minutes per step, with a heartbeat every 30 seconds for long-running steps.
- Authentication or credential errors are classified as `skip`; genuine failures and timeouts are classified as `fail`.

### `run.yaml` format

`test/e2e/run.yaml` contains two sections:

- `agents` defines executable backends with `tool`, `command`, `model`, `args`, and optional `env_required`.
  - `args` supports `{model}`, `{prompt}`, and `{sandboxDir}` interpolation.
  - Supported tools and installed skill roots are: `claude -> .claude/skills`, `codex -> .agents/skills`, `gemini -> .gemini/skills`, `opencode -> .opencode/skills`, `copilot -> .github/skills`, and `pi -> .pi/skills`.
- `scenarios` maps each scenario to an agent and may contain:
  - `fixture`: a directory overlaid onto the sandbox after `archetipo init`.
  - `prompts`: prompts or skills invoked through the agent; the skill name is derived from the `/...` prefix and is also used to verify that installation copied the skill.
  - `env_required`: overrides for the agent's environment requirements.
  - `archetipo_pre_commands`: CLI commands run before prompts.
  - `archetipo_post_commands`: CLI commands run after prompts.
  - `verify_integrate`: spec codes whose worktree integration must be verified. It captures the branch tip before post-commands, so it fits only scenarios where a post-command performs the integration.
  - `verify_spec_status`: a spec-code-to-status map asserted from `spec show` after the post-commands. Use it when the agent itself performs the final transition and no post-command exit code can carry the proof.
  - `verify_worktree_cleanup`: entries of `spec`, `branch`, and `worktree` asserting that an integration the agent already performed cleared the spec metadata and removed both branch and worktree. It is `verify_integrate`'s counterpart without the pre-integration tip comparison.
  - `verify_wiki_bootstrap`: expectations for core DDD pages, optional sources represented as `references/` concepts, `generated` state, issues, and targeted content; it also runs `wiki validate --profile bootstrap`.
  - `verify_review_wiki`: first commits configured fixture evidence, then seeds CLI approvals and commits only their page metadata plus Wiki `index.md`/`log.md`. It captures that seeded-review commit, requires every seeded page to be persisted reviewed, fresh, structurally valid, and free of `WIKI_EVIDENCE_CHANGED` before prompts, then verifies `archetipo-review` from machine effects: the exact expected persisted page set is reviewed, required-ready and explicitly reconfirmed affected-only pages appear in the dedicated approval commit, reconfirmed pages preserve their semantic content hash while advancing evidence review metadata and clearing `WIKI_EVIDENCE_CHANGED`, context-only references remain fresh and outside `wiki affected`, configured implementation artifacts have exact committed content, the spec reaches `DONE`, branch/worktree metadata and resources are removed, validation is clean, and the integrated checkout has no tracked or untracked Wiki changes.
  - Every known `verify_review_wiki` field is type-checked before the runner builds the CLI; all configured filesystem paths use one Windows/macOS/Linux-safe project-relative grammar. Unknown extension keys are retained for forward compatibility.
- Pre/post commands are split with `line.split(/\s+/)`. Avoid arguments that require complex shell quoting.

### Scenario execution sequence in `run.mjs`

1. Verify that `agent.command` exists and required `env_required` variables are present.
2. Run `archetipo init --tool <tool> --connector file --yes` in the sandbox as a non-interactive baseline.
3. Verify `.archetipo/config.yaml`, `.archetipo/shared-runtime.md`, and the skills required by the prompts.
4. Overlay the fixture, when configured. The fixture's `.archetipo/config.yaml` is authoritative and determines connector, worktree, paths, and related settings; do not add runner flags for these.
5. Initialize a Git repository in the sandbox on `main`, configure a local identity, stage only `seed_baseline_paths` when configured, and commit the generated fixture baseline. Installed skills and the copied binary remain untracked.
6. For focused review fixtures, approve `seed_reviewed_pages` only after their evidence is Git-tracked, stage only those reviewed page files plus Wiki `index.md`/`log.md`, commit the seeded-review baseline, and capture its exact hash in scenario context and reports.
7. Before any pre-command or prompt, run Wiki status and validation and require each seeded page to be persisted reviewed, fresh, structurally valid, and free of `WIKI_EVIDENCE_CHANGED`.
8. Run any `archetipo_pre_commands` using the CLI copied into the sandbox.
9. Run prompts through the agent with interpolated arguments.
10. For `verify_integrate`, capture the branch, worktree, and tip before post-commands using `spec show` and `git rev-parse`.
11. Run any `archetipo_post_commands`.
12. Verify integration: the spec is `DONE`, the pre-integration tip is reachable from `main`, the per-spec branch is deleted, and the worktree directory has been removed and no longer appears in `git worktree list --porcelain`.
13. Assert any `verify_spec_status` expectation from `spec show`, then any `verify_worktree_cleanup` entry: cleared `branch`/`worktree`/`fork_base` metadata, a deleted branch, and a worktree absent from both disk and `git worktree list --porcelain`.
14. For focused Wiki review scenarios, compare unchanged review metadata to the captured seeded-review baseline, verify acceptance from persisted/committed effects (including exact artifact content and exact approval commit paths), then assert branch/worktree cleanup directly without relying on a natural-language verdict.

### Current scenarios

- `inception-creates-valid-prd`: fixture `fixtures/inception`, prompt `/archetipo-inception`, then `validate prd`; verifies that the skill generates and persists a structurally valid PRD.
- `wiki-bootstrap-codebase-only`: fixture `fixtures/wiki-codebase`, prompt `/archetipo-wiki`; verifies a complete codebase-first DDD map without product documents or automatic approval.
- `wiki-bootstrap-prd-conflict`: fixture `fixtures/wiki-prd-conflict`, prompt `/archetipo-wiki`; verifies the `references/prd` concept, code authority for current state, and a conflict recorded as an issue.
- `from-prd-to-plan`: fixture `fixtures/prd`, prompts `/archetipo-spec` and `/archetipo-plan US-001`; covers PRD -> backlog/spec -> plan.
- `jira-init`: fixture `fixtures/jira-prd`, currently without prompts; uses the `jira` connector configuration.
- `from-plan-to-implement`: fixture `fixtures/plan`, prompt `/archetipo-implement US-001`; worktrees disabled.
- `worktree-from-plan-to-implement-integrate`: fixture `fixtures/worktree-plan`, prompt `/archetipo-implement US-001`, then `spec integrate US-001`; verifies integration.
- `worktree-implement-no-integrate`: fixture `fixtures/worktree-plan`, pre-command `spec start US-001`, then `/archetipo-implement US-001`; leaves the work unintegrated.
- `autopilot-worktree-full`: fixture `fixtures/worktree-plan`, prompt `/archetipo-autopilot US-001`; verifies the full autonomous run — plan, implement, autonomous acceptance — reaches `DONE` and leaves no branch or worktree behind.
- `autopilot-in-context-full`: fixture `fixtures/plan` (worktrees disabled), prompt `/archetipo-autopilot US-001`; verifies the same run reaches `DONE` through `spec move` rather than integration.
- `worktree-review-accepts-wiki`: creates a generated-evidence baseline commit, then a separate seeded-review metadata commit and verifies both seeded pages are fresh before prompting. It implements an exact greeting change with one required generated page, one affected-only tracked behavioral page, and one reviewed reference whose shared hub source is context; review approves the required page, explicitly reconfirms the verified-accurate affected-only page, preserves the fresh reference, commits exactly the accepted Wiki paths, integrates the spec, and removes its branch/worktree.

### Available fixtures

- `fixtures/inception`: `file` connector, worktrees disabled, and no initial PRD; used to verify generation through `/archetipo-inception`.
- `fixtures/wiki-codebase`: a small TypeScript/Express service without a PRD, including routes and tests; used for codebase-first Wiki bootstrap.
- `fixtures/wiki-prd-conflict`: a TypeScript/Express service with an intentionally inconsistent PRD (Python/FastAPI/MongoDB); used to verify conflict handling.
- `fixtures/prd`: `file` connector, worktrees disabled, and a `docs/PRD.md` for the match5 product.
- `fixtures/plan`: `file` connector, worktrees disabled, and backlog/spec/plan `US-001`, which requests `hello.txt` containing `Hello from ARchetipo`.
- `fixtures/worktree-plan`: equivalent to `plan`, but with `worktree.enabled: true`, `base: main`, `dir: .archetipo/worktrees`, and `branch_prefix: archetipo/`.
- `fixtures/worktree-wiki-review`: a worktree spec and plan with a required generated `overview`, a reviewed behavioral page tracking the changed greeting hub, and a reviewed PRD reference that marks that hub context; used to verify the reason-aware code + Wiki gate.
- `fixtures/jira-prd`: `jira` connector with `base_url: https://agilereloaded.atlassian.net/`, `story_type: Task`, `subtask_type: Sub-task`, and `priority_map`; `project_key` and `status_map` are intentionally omitted to let the CLI perform auto-discovery and auto-matching.

### Standalone smoke tests

- `npm run test:e2e:unit`: credential-free Node tests for the known-field/unknown-extension manifest contract and the two-commit focused Wiki baseline, including exact commit paths and pre-prompt freshness.
- `npm run test:web`: credential-free Node tests (`node --test`) for the browser-side modules served by `archetipo view` — `test/web/*.test.mjs`. The modules under test are pure UMD renderers with no DOM and no network, so the suite needs neither a browser nor a running viewer; it is the level at which "the frontend contains no process rules" is asserted, for instance that `workspace-status.js` renders labels it receives without ever naming a stage or an action itself.
- `npm run test:view-workspace-status-smoke`: builds the CLI, initializes a sandbox and starts `archetipo view` on a random port, then walks the workspace through three states — empty, with a `docs/PRD.md`, with a backlog — and verifies through `GET /api/workspace/status` that the declared stage and the recommended next step follow the real state of the workspace: `senza-prd` recommending the workspace-scoped `inception`, then the workspace-scoped backlog generation, then the spec-scoped planning naming the first `TODO` card of `GET /api/board`. The oracle for "without restarting the viewer" is the viewer PID, asserted unchanged after every transition, together with the `board_changed` frame received on the existing SSE channel when the backlog appears. Credential-free, no AI agent involved (the smoke runs no action — it writes artifacts and watches the answer change), no traffic outside `127.0.0.1`, and no arbitrary sleep: every wait polls a viewer route with an explicit timeout naming what was expected and what arrived.
- `node ./test/e2e/validate-inception-smoke.mjs`: builds the CLI, initializes a file/pi sandbox, writes an invalid PRD, verifies `archetipo validate prd` exits with `0` and returns `kind=validation_result`, `data.ok=false`, `PRD_PLACEHOLDER_LEFT`, and `PRD_MISSING_SECTION`; then writes a valid PRD and verifies `kind=validation_result` with `data.ok=true`. Produces an HTML report. Options: `--workspace-root`, `--cleanup`. Note: the help text mentions `npm run test:validate-inception`, but no corresponding package script currently exists.
- `npm run test:view-workspace-smoke`: builds the CLI, initializes a host sandbox, starts `archetipo view` on a random port, and verifies through the HTTP API and the filesystem that a workspace is created *elsewhere* from the UI. `GET /api/workspace/options` is the only source of the offered choices — connectors, tools, default paths and default worktree settings, plus the installed process Archetype reported as one identity and never as a catalogue — and `POST /api/workspace` accepts exactly what that contract offers: a creation with non-default paths and `worktree.enabled: true` writes them into the new `.archetipo/config.yaml`, installs `shared-runtime.md` and every Archetype skill under each selected tool directory, and is read back by `archetipo config show` run inside the created directory. An unknown connector, an unknown tool, an already initialized destination, a destination that is a file and a destination that is not writable each answer `400` with a `fields[]` entry naming the offending field and a stable `WORKSPACE_*` code, and the recursive listing of the destination is identical before and after. A failure forced halfway through the commit (a file where a tool's skills directory must be created) leaves no `.archetipo/`, no created directory and no damaged pre-existing file, which is how the no-partial-workspace guarantee is observed rather than assumed. The workspace the viewer itself serves is asserted unchanged by the whole run. The unwritable-destination case is skipped with an explicit reason when the tests run as root or on Windows, where `0o500` does not refuse the write. Credential-free, no traffic outside the local machine, and no arbitrary sleep — every wait polls a viewer route.
- `npm run test:view-delete-smoke`: builds the CLI, initializes a sandbox, adds two specs, seeds plan/review artifacts for `US-901`, starts `archetipo view` on a random port, and verifies through the HTTP API that `DELETE /api/spec/US-901` removes that card, retains `US-902`, subsequently returns 404 for `US-901`, and deletes its spec/plan/review artifacts.
- `npm run test:view-execution-smoke`: builds the CLI, initializes a sandbox with two specs (one left `TODO`, one moved to `PLANNED` by a real `spec plan`), starts `archetipo view` on a random port with a sentinel `ARCIPELAGO_TOKEN`, and verifies through the HTTP API that the provider list exposes every registered provider with its configurable fields and no credential — `arcipelago` with its five fields, and the local `codex` with the `spec.plan` and `run.dialog` capabilities, exactly `command`, `model`, `sandbox` and `timeout_seconds`, and the local `claude` with the same two capabilities and exactly `command`, `model`, `permission_mode` and `timeout_seconds`, none of them with a field whose name asks for a credential — that each provider reports a boolean `available` and that an unavailable one states a non-empty `unavailable_reason` (the smoke observes availability instead of requiring it, so it passes with or without Codex and Claude Code installed, whether or not they are logged in, and never touches `PATH`), that each local provider names in `model_field` one of its own configurable fields and then answers with either a non-empty `models` list — every entry with a non-empty `id` and exactly one marked as the provider's own `default` — or a non-empty `models_unavailable_reason`, never both and never neither, while `arcipelago` declares no catalog at all and therefore carries none of the three fields (the catalog too is observed and not required: the invariant is what must hold, so the smoke passes whether the local runtime answers or is missing), that every model option travelling inside a catalog entry carries a name, a label, at least one choice with a non-empty value and exactly one choice marked as the provider's own `default`, never collides with a configurable field of the same provider and never arrives as an empty list — and that, when the claude catalog is obtainable, it holds both cases the panel has to tell apart, at least one model declaring an option and at least one declaring none, that a valid `PUT /api/execution/provider/default` reaches `.archetipo/config.yaml` and is reported afterwards, that a model outside the catalog saved through that same route is read back unchanged, that an invalid one answers `400` naming the offending `field` while leaving both file and previous default untouched, and that the spec detail exposes exactly the actions the process Template admits — an empty list once the spec is moved to `DONE`.
- `npm run test:view-plan-smoke`: builds the CLI, initializes a sandbox with two `TODO` specs, starts a fake ARcipelago hub on `127.0.0.1` and `archetipo view` on a random port with a sentinel `ARCIPELAGO_TOKEN`, saves that hub as the workspace default execution provider, and verifies through the HTTP API that pressing the `plan` action starts exactly one execution (a second `POST /api/spec/US-901/execution` answers `409` naming the running one, and only one record file exists), that the non-terminal state is observable while the board keeps being served, that the run the hub really planned ends `SUCCEEDED` with the spec `PLANNED` and its plan readable, that the run the hub fails ends `FAILED` with the remote reason and leaves its spec `TODO` without a plan, and that a reload finds both executions again without starting a second one. Only the hub is fake: the viewer, the connector, the ARcipelago provider and its receipt verification are the real ones, and the smoke requires no credentials and no traffic outside `127.0.0.1`.
- `npm run test:view-run-smoke`: builds the CLI, initializes a sandbox with one `TODO` spec, starts a fake ARcipelago hub on `127.0.0.1` and `archetipo view` on a random port with a sentinel `ARCIPELAGO_TOKEN`, saves that hub as the workspace default execution provider, presses the `plan` action and then binds the remote task to a run, and verifies through the four run routes of the viewer that the ordered history is applied without visible duplication (a frame re-sent on the live stream appears once, and `after_id=<last_id>` is empty), that a broken stream is resumed from the cursor the projection already holds (the second subscription the hub records carries `afterId=4`, and the history stays gap-free), that a message is delivered to the hub and enters the timeline only once the hub republishes it, that a pending approval opened mid-run on a healthy stream is exposed with its options verbatim without any reconnection (the subscription count the hub records is unchanged) and its answer is recorded at the hub and re-read as no longer pending, that a cancel reports the state the hub confirms and a run the hub closed is never reopened by a retry (`409 run_not_active`), and that six refused commands (`404` and `409 run_not_active` on each of the three commands) leave the projection byte-identical. The credential travels to the hub — the event stream included — and never appears in any viewer response. Only the hub is fake: the viewer, the connector, the ARcipelago provider, its SSE consumer and the server-side run follower are the real ones, the smoke requires no credentials and no traffic outside `127.0.0.1`, and it contains no arbitrary sleep — the hub progresses only when the test commands it and every wait polls a viewer route. The four viewer routes it covers are:
  - `GET /api/execution/{id}/run?after_id=<n>`: the server-side projection of the run behind an execution — `run` (or `null` when the remote work has no run yet), the events after the cursor, `last_id`, the pending `approvals`, `connected`, `truncated` and an optional `notice`. The projection is maintained by one server-side follower per execution: it holds the single SSE consumer towards the hub, resumes it from its own high-water `id` after a drop, and re-reads the pending approvals on a timer, because the event stream carries no approval-bearing frame and an approval opened mid-run would otherwise stay invisible until the stream broke.
  - `POST /api/execution/{id}/run/messages` with `{"message":"…"}`: delivers an operator message and answers `202` with the projection, which deliberately does not yet contain the message.
  - `POST /api/execution/{id}/run/approvals/{approvalId}` with `{"option_id":"…"}`: answers one pending approval and returns `202` with the projection re-read after the answer.
  - `POST /api/execution/{id}/run/cancel`: asks the runner to close the session and returns `202` with the state the provider reports, never a locally derived terminal one.
  A refusal is rendered from the provider's reason and never from its message: `not_found` answers `404`, `run_not_active`, `runner_offline` and `unauthorized` answer `409`, and `unsupported` answers `400`.
- `npm run test:view-local-run-smoke`: builds the CLI once and then runs **the same script once per local provider** — `codex` and `claude` — reporting the outcome provider by provider; `-- --provider <id>` runs one of them alone. For each provider it initializes a sandbox with `archetipo init --tool <provider>` (which is also what installs the planning skill the prompt invokes) and one `TODO` spec, starts `archetipo view` on a random port with a sentinel session-material environment variable, and saves as workspace default the real provider pointed at a **fake agent binary**: `test/e2e/support/fake-codex.mjs`, which answers `--version` and speaks the real `codex app-server --listen stdio://` JSON-RPC protocol on stdio, and `test/e2e/support/fake-claude.mjs`, which answers `--version` and speaks the real Claude Code stream-json protocol — NDJSON on stdin and stdout, `system`/`init`, `assistant`, `user` and `result` frames outward, an operator `user` frame and a `control_request` of subtype `interrupt` inward. It verifies that the provider declares `run.dialog` beside `spec.plan` and exposes exactly its own configuration fields (`command`, `model`, `sandbox`, `timeout_seconds` for codex; `command`, `model`, `permission_mode`, `timeout_seconds` for claude); that the session is opened as configured — `approvalPolicy: never` with the configured sandbox for codex, and for claude the streaming invocation `--print --input-format stream-json --output-format stream-json --verbose --replay-user-messages --no-session-persistence --permission-mode auto` — and that the prompt names the spec; that the frames become an ordered history (`text`, `tool_start`, `tool_end`, `text`) whose cursor returns exactly what is new; that a message posted to `POST /api/execution/{id}/run/messages` reaches the process — as a `turn/steer` naming the turn in progress for codex and as a `user` frame for claude, which is the single statement that legitimately differs between the two and is carried as a predicate of the provider description — is absent from the `202` body, and appears exactly once only after the process re-emits it as a user message; that `POST .../run/cancel` reports the run still `ACTIVE` while the process resists and only the end of the turn changes the state; that message and cancel are refused with `404` on an execution this workspace does not hold and with `409 run_not_active` on a run that is over, and that those four refusals leave the projection byte-identical; and that the sentinel never appears in any viewer response nor in `.archetipo/config.yaml`. Only the agent binary is fake — the viewer, the connector, the provider, its protocol client, the local session and the run follower are the real ones — the smoke needs no credentials and no traffic outside `127.0.0.1`, each fake never progresses on its own (every frame it emits is commanded through a local control server, and every frame it receives is reported back to it), and there is no arbitrary sleep. It is skipped on Windows, where the fakes' POSIX shebang does not apply.
- `npm run test:view-run-model-smoke`: builds the CLI and verifies, against one sandbox served by the real `archetipo view` and a **fake agent binary** (`test/e2e/support/fake-claude.mjs`, driven frame by frame through a local control server), that a single run can use a model other than the one the workspace configures without the workspace ever moving. The sandbox is initialized with `archetipo init --tool claude` — which installs the planning skill the prompt invokes — holds one `TODO` spec, and saves through `PUT /api/execution/provider/default` the real `claude` provider pointed at the fake with `model: sonnet` and `effort: high`, with a sentinel session-material environment variable in the viewer's environment. It verifies that `GET /api/execution/model-choice` answers, before anything is started, `available:true` with `model: sonnet`, `model_source: workspace`, `options: {effort: high}`, `model_field: model` and a catalog offering `opus`, `sonnet` and `haiku` with the `effort` option declared on `opus`; that `POST /api/spec/US-901/execution {"action":"plan","model":"opus","model_options":{"effort":"low"}}` answers `201` and hands the process an invocation carrying `--model opus` and `--effort low` as adjacent flag/value pairs with neither `sonnet` nor `high` anywhere in it; that `GET /api/execution/{id}` reports `model_choice: {model: opus, options: {effort: low}, source: run}` while the run is still open and still reports it after the terminal write; that `.archetipo/config.yaml` is byte-for-byte identical before and after that run and `GET /api/execution/providers` still serves `model: sonnet` with `effort: high` as the persisted default; that the next start, sent without either field, is invoked with `--model sonnet --effort high` and records `source: workspace`; and, as a regression, that a run started after an overridden one still delivers an operator message to the process as a `user` frame and shows it exactly once, only after the process re-emits it. The oracle is deliberately the argument list the process really received, because `buildArgs` appends `--model` and `--effort` only for non-empty values, so a per-run choice that had silently fallen back to the workspace configuration would be visible as `sonnet`/`high` in that list. The sentinel never appears in any viewer response nor in `.archetipo/config.yaml`. Both planning runs close `FAILED` on purpose — the fake persists no plan — because what this smoke asserts is which model reached the process, not that a plan exists. Only the agent binary is fake — the viewer, the connector, the claude provider, its stream-json client, the local session and the run follower are the real ones — the smoke needs no credentials and no traffic outside `127.0.0.1`, the fake never progresses on its own, and there is no arbitrary sleep: every wait polls a viewer route or the control server. It writes `report.html` and `summary.json` into the run directory, and is skipped on Windows, where the fake's POSIX shebang does not apply.
- `npm run test:view-workspace-root-smoke`: builds the CLI and verifies, against two real workspaces `A` and `B` served by the real `archetipo view` and a **fake agent binary** (`test/e2e/support/fake-claude.mjs`, driven frame by frame through a local control server), that a run acts on the workspace open in the UI and never on the directory the viewer was launched from. Both workspaces are initialized with `archetipo init --tool claude` and hold one recognisable spec code of their own; the viewer is started **inside `A`**, `B` is registered with `POST /api/workspaces` and opened with `POST /api/workspaces/{id}/open` on the same PID, and the real `claude` provider pointed at the fake is saved on `B` through `PUT /api/execution/provider/default`. It verifies that `POST /api/spec/US-B01/execution {"action":"plan"}` starts the agent process **in the project root of `B`** — the oracle is the working directory the process itself reports at startup, that is its real `cmd.Dir`, not a viewer field standing in for it — that a file the agent writes relative to its own working directory appears under `B` and not under `A`, that `GET /api/execution/{id}` reports that same directory as `working_dir`, and that the recursive listing of `A` and the viewer PID are both unchanged, so "the first action after opening `B` runs in `B`, without restarting the process" is demonstrated and not asserted; that opening `A` again while that run is still in flight leaves its persisted record in `B` still naming `B` as its working directory and leaves `A` without a single execution record or artifact, so a run never migrates to a workspace opened after it started; and that with `B` reopened and its directory then renamed away on the filesystem, both `POST /api/spec/{code}/execution` and `POST /api/workspace/execution` answer `409` with a message **naming that directory**, while the renamed directory holds no execution record beyond the ones it already had. Only the agent binary is fake — the viewer, the connector, the claude provider, its stream-json client and the local session are the real ones — the smoke needs no credentials and no traffic outside `127.0.0.1`, the fake never progresses on its own, and there is no arbitrary sleep: every wait polls a viewer route or the control server. It writes `report.html` and `summary.json` into the run directory, and is skipped on Windows, where the fake's POSIX shebang does not apply.
- `npm run test:view-conversation-smoke`: builds the CLI and verifies, against three real workspaces served by the real `archetipo view` and a **fake agent binary** (`test/e2e/support/fake-claude.mjs`, driven frame by frame through a local control server), that a person can talk to the agent about the workspace they have open. Two of them, `A` and `B`, are initialized with `archetipo init --tool claude` and served by a single viewer process launched inside `A`, with the real `claude` provider pointed at the fake saved through `PUT /api/execution/provider/default` and a sentinel session-material environment variable in the viewer's environment; the third is initialized with `archetipo init --tool codex` and holds the real `codex` provider pointed at `test/e2e/support/fake-codex.mjs`, which answers `--version` like the real binary and is therefore **available and simply not conversational**. It verifies six facts, one per criterion: that `POST /api/workspace/conversation` answers `201` and starts the agent process **in the project root of the open workspace** — the oracle is the working directory the process itself reports at startup, that is its real `cmd.Dir`, not a viewer field standing in for it — under the streaming invocation `--print --input-format stream-json --output-format stream-json --verbose --replay-user-messages --no-session-persistence`, that an `assistant` frame becomes a `text` event on `GET /api/workspace/conversation`, and that a message posted to `POST /api/workspace/conversation/messages` reaches the process as a `user` frame, is absent from the `202` body, is still absent while the process merely holds it, and enters the history exactly once only after the process re-emits it; that the whole exchange is **not an execution of the process** — the payload of `GET /api/board` is byte-identical to the one read before the conversation, `GET /api/workspace/actions` still answers `execution: null`, and `.archetipo/executions/` holds no record at all; that a reload — the second `GET …?after_id=0` the browser makes — finds the same conversation id and the whole history with `truncated:false` on the **same viewer PID**, asserted unchanged, and that once more events have been produced than the 2000-event retention window keeps, a read from a cursor now outside that window answers `truncated:true` beginning at the oldest surviving event with a non-empty `notice` saying how many were dropped; that on the codex workspace the conversation is **not offered** — `GET` answers `200` with `available:false` and an `unavailable_reason` naming `workspace.converse`, `POST` answers `409` with the identical sentence, no agent process is ever started (the control server records nothing) and the recursive listing of the sandbox is unchanged; that the conversation **does not follow a workspace switch** — after `POST /api/workspaces/{B}/open` on the same PID the read answers `conversation: null` with the id of the conversation of `A` nowhere in the payload, the agent process of `A` has been released, and a conversation opened in `B` starts its own process in the project root of `B`; and that `DELETE /api/workspace/conversation` answers `200` with the closed conversation still readable in state `CLOSED`, **terminates the agent process** — asked of the operating system by the pid the process itself reported, never inferred from the route — after which a later message and a second close both answer `409` with a reason. Every refusal (`404` on an unknown workspace, `409` on a second open, on a message after the close and on a second close) is asserted to leave the projection byte-identical, read before and after. The sentinel never appears in any viewer response nor in any `.archetipo/config.yaml`. Only the agent binary is fake — the viewer, the connector, the claude provider, its stream-json client, the bounded local session and the conversation lifecycle are the real ones — the smoke needs no credentials and no traffic outside `127.0.0.1`, the fake never progresses on its own, and there is no arbitrary sleep: every wait polls a viewer route, the control server or the operating system with an explicit timeout naming what was expected and what arrived. It writes `report.html` and `summary.json` into the run directory, and is skipped on Windows, where the fakes' POSIX shebang does not apply.
- `npm run test:view-no-workspace-smoke`: builds the CLI and verifies, against three real workspaces (`alfa`, `beta`, and a third registered then moved away) and a launch directory created outside the repository — the repository is itself an ARchetipo workspace, so a launch point under `test/workspaces/` would resolve *up* to it — that `archetipo view` started outside every workspace opens the known-workspaces home instead of a board. Everything is real except the browser: the viewer, the workspaces, and the known-workspaces registry, isolated per run through `ARCHETIPO_STATE_DIR` inside the run directory; nothing is simulated and no AI agent is involved. The oracles are the HTTP contract and the filesystem: `GET /api/workspaces` answering `open:false` with an empty `currentPath` and three entries carrying name, path, last access and probed reachability (two `reachable`, one `missing`) with none marked `current`; every workspace-scoped route (`/api/board`, `/api/metrics`, `/api/workspace/status`, `/api/workspace/actions`, `/api/prd`, `/api/config`, `/api/execution/providers`, `/api/mockups`, `POST /api/workspace/execution`) answering `409` with `workspaceOpen:false` and none of the content keys `columns`, `specs`, `providers`, `stage`, `content`; `workspaces.json` **byte-for-byte identical** after the neutral launch, with the launch directory absent from the entries; add, forget and create still working from the home, the forgotten directory left with an identical recursive listing; the open of `beta` returning `current:true` on the **same viewer PID**, with the board switching to the spec code that exists only in `beta` and `lastOpenedAt` moving; and a restart inside `beta` — from its root and from a directory below it — reporting `open:true` on the very first list served, with no open request ever sent. Credential-free, no traffic outside `127.0.0.1`, and no arbitrary sleep: every wait polls a viewer route with an explicit timeout naming what was expected and what arrived. Each run writes `report.html` and `summary.json` in its run directory.
- `npm run test:view-inception-smoke`: builds the CLI and verifies, against three sandboxes served by the real `archetipo view` and a **fake agent binary** (`test/e2e/support/fake-claude.mjs`, driven frame by frame through a local control server), that a person can start the inception from the UI and obtain the PRD without knowing the skills. Each sandbox is initialized with `archetipo init --tool claude` — which is what installs the inception skill the prompt invokes — and saves as workspace default the real `claude` provider pointed at the fake, with a sentinel session-material environment variable in the viewer's environment. It verifies that a workspace without a PRD answers `GET /api/workspace/actions` with `has_prd:false`, the `inception` action `runnable:true` and no execution behind it, and that `POST /api/workspace/execution {"action":"inception"}` answers `201` with a `RUNNING` record whose `spec_code` is empty and hands the process a prompt naming `/archetipo-inception`; that the agent's question, emitted as an `assistant` frame followed by a `result` that carries no receipt, becomes a `text` event on `GET /api/execution/{id}/run` while the record stays `RUNNING` — a turn that ends on a question does not end an inception — and that the answer posted to `POST /api/execution/{id}/run/messages` reaches the process as a `user` frame, is absent from the `202` body and enters the same conversation exactly once only after the process re-emits it; that once the document is persisted (through `archetipo prd write`, a command of the smoke and never of the fake) and the agent closes on the receipt line `{"artifact":"prd","status":"WRITTEN","path":"docs/PRD.md"}`, the record becomes `SUCCEEDED`, `docs/PRD.md` holds the document on disk and `GET /api/prd` serves it back from the **same** viewer process, never restarted; that a run stopped with `POST /api/execution/{id}/run/cancel` after a partial `docs/PRD.md` was written closes `FAILED` with a non-empty reason, leaves no `docs/PRD.md` and an empty `GET /api/prd`, so a first inception either lands whole or leaves no trace; and that a workspace that already has a PRD is offered the action as `runnable:false` with its reason, answers `409` when it is pressed anyway, keeps its document byte-identical and writes no record under `.archetipo/executions/`. The sentinel never appears in any viewer response nor in any `.archetipo/config.yaml`. Only the agent binary is fake — the viewer, the connector, the claude provider, its stream-json client, the local session, the run follower and the effect verification are the real ones — the smoke needs no credentials and no traffic outside `127.0.0.1`, the fake never progresses on its own, and there is no arbitrary sleep: every wait polls a viewer route or the control server. It writes `report.html` and `summary.json` into the run directory, and is skipped on Windows, where the fake's POSIX shebang does not apply.
- `npm run test:view-backlog-smoke`: builds the CLI and verifies, against three sandboxes served by the real `archetipo view` and a **fake agent binary** (`test/e2e/support/fake-claude.mjs`, driven frame by frame through a local control server), that a person can generate the initial product backlog from the UI once the PRD exists. Each sandbox is initialized with `archetipo init --tool claude` — which is what installs `.claude/skills/archetipo-spec/`, the skill the prompt invokes and whose absence would make the conversation impossible — and saves as workspace default the real `claude` provider pointed at the fake, with a sentinel session-material environment variable in the viewer's environment. It verifies that a workspace with a PRD and no backlog answers `GET /api/workspace/actions` with `has_prd:true`, `has_backlog:false`, the `backlog` action `offered:true` and `runnable:true` and no execution behind it, and that `POST /api/workspace/execution {"action":"backlog"}` answers `201` with a `RUNNING` record whose `spec_code` is empty and hands the process a prompt naming `/archetipo-spec`; that the agent's question, emitted as an `assistant` frame followed by a `result` that carries no receipt, becomes a `text` event on `GET /api/execution/{id}/run` while the record stays `RUNNING` — a turn that ends on a question does not end a generation — and that the answer posted to `POST /api/execution/{id}/run/messages` reaches the process as a `user` frame, is absent from the `202` body, is still absent from the conversation while the process holds it, and enters it exactly once only after the process re-emits it; that once three specs in two epics are persisted (through `archetipo spec add`, a command of the smoke and never of the fake) and the agent closes on the receipt line `{"artifact":"backlog","status":"WRITTEN","epics":2,"specs":3}`, the record becomes `SUCCEEDED`, the three `.archetipo/specs/<code>.yaml` files are on disk, `GET /api/board` serves the three specs with their two epics from the **same** viewer process, never restarted, and the action stops being `offered`; that a run stopped with `POST /api/execution/{id}/run/cancel` after a single spec was persisted closes `FAILED` with a non-empty reason and is rolled back — no spec file and no `.archetipo/backlog.yaml` survive, the board is empty on the same viewer and the action is `offered` again, so a first generation either lands whole or leaves no trace; and that a workspace that already has a backlog is offered it as `offered:false` with its reason, answers `409` with that very sentence when it is pressed anyway, keeps every backlog file byte-identical and writes no record under `.archetipo/executions/`. The sentinel never appears in any viewer response nor in any `.archetipo/config.yaml`. Only the agent binary is fake — the viewer, the connector, the claude provider, its stream-json client, the local session, the run follower, the effect confirmation and the partial-backlog rollback are the real ones — the smoke needs no credentials and no traffic outside `127.0.0.1`, the fake never progresses on its own, and there is no arbitrary sleep: every wait polls a viewer route or the control server. It writes `report.html` and `summary.json` into the run directory, and is skipped on Windows, where the fake's POSIX shebang does not apply.
- `npm run test:view-spec-draft-smoke`: builds the CLI and verifies, against three sandboxes served by the real `archetipo view` and a **fake agent binary** (`test/e2e/support/fake-claude.mjs`, driven frame by frame through a local control server), that a person can obtain a spec from the agent and confirm it, beside the manual form and without the agent ever writing anything. Each sandbox is initialized with `archetipo init --tool claude` — which is what installs `.claude/skills/archetipo-spec/`, the skill the prompt invokes — and saves as workspace default the real `claude` provider pointed at the fake, with a sentinel session-material environment variable in the viewer's environment. It verifies that a workspace with a backlog answers `GET /api/workspace/actions` with the `spec-draft` action `offered:true` and `runnable:true` and no execution behind it, and that `POST /api/workspace/execution {"action":"spec-draft"}` answers `201` with a `RUNNING` record whose `spec_code` is empty and hands the process a prompt that names `/archetipo-spec` **and forbids `archetipo spec add` and any other write**; that the agent's question, emitted as an `assistant` frame followed by a `result` that carries no receipt, becomes a `text` event on `GET /api/execution/{id}/run` while the record stays `RUNNING` — a turn that ends on a question does not end a proposal — and that the answer posted to `POST /api/execution/{id}/run/messages` reaches the process as a `user` frame, is absent from the `202` body, is still absent while the process holds it, and enters the conversation exactly once only after the process re-emits it; that the closing receipt line `{"artifact":"spec_draft","status":"PROPOSED",…}` makes the record `SUCCEEDED` and `GET /api/execution/{id}` serve the whole proposal back under `result.payload.spec_draft` — title, epic, priority, points, scope, blockers and the multi-line markdown body intact — while every backlog file on disk stays byte-identical and `GET /api/board` still shows only the pre-existing specs, so a proposal is provably not a creation; and that only the subsequent `POST /api/spec` carrying that proposal creates the spec, served back in `TODO` by the **same** viewer process, never restarted. A second sandbox proves the cancellation: a run that wrote a spec anyway and is then stopped with `POST /api/execution/{id}/run/cancel` closes `FAILED` naming the spec it took back, that spec is gone from disk and from the board, the pre-existing backlog is byte-identical, and the next real creation receives the very code the cancelled run had taken — which is what "the progressive code was not consumed" means, since there is no counter anywhere, only the persisted backlog. A third sandbox proves that a workspace with no backlog is offered the action as `offered:false` with its reason, answers `409` with that same sentence when it is pressed anyway, writes no record under `.archetipo/executions/` and has an identical recursive listing before and after. The sentinel never appears in any viewer response nor in any `.archetipo/config.yaml`. Only the agent binary is fake — the viewer, the connector, the claude provider, its stream-json client, the local session, the run follower, the inverted effect confirmation and its rollback are the real ones — the smoke needs no credentials and no traffic outside `127.0.0.1`, the fake never progresses on its own, and there is no arbitrary sleep: every wait polls a viewer route or the control server. It writes `report.html` and `summary.json` into the run directory, and is skipped on Windows, where the fake's POSIX shebang does not apply.
- `npm run test:view-workspace-runs-smoke`: builds the CLI and verifies, against one real sandbox served by the real `archetipo view`, that the workspace shows what it is running while a spec is open. Two things are fake and only two: the ARcipelago hub, replaced by a local Node server on `127.0.0.1` that hands out a run and opens one approval by hand, and the agent binary, replaced by `test/e2e/support/fake-claude.mjs` driven frame by frame through a local control server. Everything else is real — the filefs connector, the `claude` provider with its stream-json client and its local session, the `arcipelago` provider with its SSE consumer and the server-side run follower, `GET /api/workspace/runs`, `POST /api/spec/{code}/execution`, `POST /api/workspace/execution`, `GET /api/workspace/conversation` and the assets the browser is really served. Both providers appear because neither can stand in for the other: a run of workspace scope exists only under a provider declaring the workspace capabilities, and a run waiting for a decision exists only under a provider that has approvals at all, so the workspace default is moved from one to the other through the very route the Execution panel uses. The facts it proves: `GET /api/workspace/runs` lists the two runs in flight with their scope, action and status — one spec-scoped `plan` and one workspace-scoped `spec-draft` — each carrying the execution id it is reached by, and leaves out a third run already driven to a terminal record; the approval opened at the hub marks **only** that run `awaiting_response` and names the pending decision, which is asserted **by counting** — every viewer request is recorded and the assertion is that zero of them are `GET /api/execution/{id}/run` for that execution, so "you learn a run is blocked without having opened it" is a count and not a comment — and answering the decision through the existing route clears the mark; the open conversation answers with the same id, the same history and the same cursor after the spec detail routes have been exercised, on a viewer PID asserted unchanged; the served `index.html` nests `#workspace-conversation` and `#workspace-runs` inside `<aside id="workspace-rail">`, keeps `#modal-root` out of the rail and inside the primary column of `#workspace-shell`, and still carries all eleven identifiers of the spec operations and the three `data-tab` values; the served `app.css` carries the media query at the breakpoint `workspace-layout.js` declares — read from the module, never retyped here — and styles every class that module returns for a narrow state. Credential-free, no traffic outside `127.0.0.1`, nothing progresses on its own and there is no arbitrary sleep: every wait polls a viewer route with an explicit timeout naming what was expected and what arrived. It writes `report.html` and `summary.json` into the run directory, and is skipped on Windows, where the fake's POSIX shebang does not apply.
- `npm run test:wiki-smoke`: builds the CLI, inspects a sandbox codebase, initializes the Wiki, creates ordinary and decision/reference pages, then verifies validation, unsafe-path errors, root-source affected matching, ADR search, cataloging, selective approval, tracked-versus-context freshness, explicit reconfirmation, distinct unreadable-evidence findings, and index regeneration. This credential-free smoke runs unchanged in the Ubuntu/macOS/Windows CI test matrix after Node setup.

When adding or modifying E2E tests, prefer explicit fixtures with a complete `.archetipo/config.yaml`, use `env_required` for external credentials, keep generated reports out of commits, and update this section whenever runner semantics change.

## Installation for end users

Primary path for any system with Node.js:

```bash
npm i -g @techreloaded/archetipo     # Global CLI in PATH
archetipo init [--tool …] [--connector …]
```

The Node shim in `npm/archetipo/bin/archetipo.js` resolves the binary package for the current platform, sets `ARCHETIPO_DATA_DIR`, and spawns the Go binary. Bundled skills live in `npm/archetipo/skills/` and are copied by `archetipo init` into `.{tool}/skills/` in the project.

## Operational notes

- `.archetipo/config.yaml` in this repository is a **template** copied into the target project as `.archetipo/config.yaml`.
- The `file` connector is the default and uses local Markdown files. The `github` connector requires an authenticated `gh` CLI.
- See the E2E tests section above for local E2E instructions.
